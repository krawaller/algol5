import test from '../gentester'
import lib from '../../../src/codegen/'

describe("The flow start stuff",()=>{
    test(F,'prepareStartingStep',{

    })
    test(F,'saveStartingStep',{

    })
    test(F,'applyStartingConsequences',{

    })
});
