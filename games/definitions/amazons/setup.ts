import {Setup} from '../../../types';
import { AmazonsUnit } from './_types';

const amazonsSetup: Setup<AmazonsUnit> = {
  queens: {
    "1": ["d10", "g10", "a7", "j7"],
    "2": ["a4", "d1", "g1", "j4"]
  }
};

export default amazonsSetup;
