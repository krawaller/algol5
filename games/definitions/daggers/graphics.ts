import {Graphics} from '../../types';

const daggersGraphics: Graphics = {
  tiles: {
    base: "playercolour"
  },
  icons: {
    daggers: "bishop",
    crowns: "king"
  }
};

export default daggersGraphics;
