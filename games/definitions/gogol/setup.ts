import {Setup} from '../../types';
import { GogolUnit } from './_types';

const gogolSetup: Setup<GogolUnit> = {
  soldiers: {
    "1": [
      ["rect", "a1", "h1"]
    ],
    "2": [
      ["rect", "a8", "h8"]
    ]
  }
};

export default gogolSetup;
