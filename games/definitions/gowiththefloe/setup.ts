import {Setup} from '../../../types';
import { GowiththefloeUnit } from './_types';

const gowiththefloeSetup: Setup<GowiththefloeUnit> = {
  seals: {
    "1": ["b2", "b7"]
  },
  bears: {
    "2": ["g2", "g7"]
  }
};

export default gowiththefloeSetup;
