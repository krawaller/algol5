import {Graphics} from '../../types';

const kickrunGraphics: Graphics = {
  tiles: {
    corners: "playercolour"
  },
  icons: {
    runners: "bishop",
    sidekickers: "pawn"
  }
};

export default kickrunGraphics;
