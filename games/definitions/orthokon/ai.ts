import {AI} from '../../types';

const orthokonAI: AI = {
  aspects: {
    headcount: ["sizeof", "myunits"]
  },
  brains: {
    Bob: {
      plus: {
        headcount: 1
      }
    }
  }
};

export default orthokonAI;
