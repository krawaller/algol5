import {Setup} from '../../types';

const orthokonSetup: Setup = {
  soldiers: {
    "1": [
      ["rect", "a1", "d1"]
    ],
    "2": [
      ["rect", "a4", "d4"]
    ]
  }
};

export default orthokonSetup;
