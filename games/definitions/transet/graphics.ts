import {Graphics} from '../../types';

const transetGraphics: Graphics = {
  icons: {
    pinets: "pawn",
    piokers: "bishop",
    piases: "king"
  },
  tiles: {
    base: "playercolour"
  }
};

export default transetGraphics;
