{
  "scripts": {
    "robot": "echo 'encoding module robot is a noop'",
    "clean": "echo 'encoding module clean is a noop'",
    "showUnusedCodes": "cd ../..; npx babel-node --extensions '.ts','.js' ./modules/encoding/commands/showUnusedCodes"
  }
}
