{
  "scripts": {
    "robot": "echo 'firebase robot is a noop'",
    "clean": "echo 'firebase clean is a noop'",
    "deployFunc": "cd functions; firebase deploy --only functions",
    "deployRules": "firebase deploy --only database",
    "postinstall": "node ./postinstall"
  },
  "dependencies": {
    "firebase": "^7.15.5",
    "firebaseui": "^4.5.1",
    "google-auth-library": "^6.0.3",
    "react-firebaseui": "^4.1.0"
  },
  "devDependencies": {
    "firebase-admin": "^9.2.0",
    "firebase-functions": "^3.11.0",
    "firebase-tools": "^8.10.0"
  }
}
