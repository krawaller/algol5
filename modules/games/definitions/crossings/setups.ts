import { CrossingsDefinition } from "./_types";

const crossingsSetupBook: CrossingsDefinition["setups"] = {
  basic: {
    soldiers: {
      1: [{ rect: ["a1", "h2"] }],
      2: [{ rect: ["a7", "h8"] }],
    },
  },
};

export default crossingsSetupBook;
