import { DaoDefinition } from "./_types";

const daoScripts: DaoDefinition["scripts"] = [
  {
    desc: "basic",
    lines: [
      {
        commands: []
      }
    ]
  }
];

export default daoScripts;
