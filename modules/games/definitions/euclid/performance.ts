import { EuclidDefinition } from "./_types";

const euclidPerformance: EuclidDefinition["performance"] = {
  canAlwaysEnd: {},
  massiveTree: {},
  noEndGameCheck: []
};

export default euclidPerformance;
