import { GekitaiMeta } from "./_types";

const gekitaiMeta: GekitaiMeta = {
  id: "gekitai",
  name: "Gekitai",
  tags: [],
  tagline: "Brown bag built!",
  source: "http://some.url.to.rules",
  code: "x", // this must be a unique character! try 'npm run checkCodes'
};

export default gekitaiMeta;
