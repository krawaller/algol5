import { HobbesDefinition } from "./_types";

const hobbesPerformance: HobbesDefinition["performance"] = {
  canAlwaysEnd: {},
  massiveTree: {},
  noEndGameCheck: []
};

export default hobbesPerformance;
