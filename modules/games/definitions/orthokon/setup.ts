import { OrthokonSetup } from "./_types";

const orthokonSetup: OrthokonSetup = {
  soldiers: {
    "1": [{ rect: ["a1", "d1"] }],
    "2": [{ rect: ["a4", "d4"] }]
  }
};

export default orthokonSetup;
