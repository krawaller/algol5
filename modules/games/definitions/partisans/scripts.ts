import { PartisansDefinition } from "./_types";

const partisansScripts: PartisansDefinition["scripts"] = [
  {
    desc: "basic",
    lines: [
      {
        commands: []
      }
    ]
  }
];

export default partisansScripts;
