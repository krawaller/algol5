import { SaposDefinition } from "./_types";

const saposScripts: SaposDefinition["scripts"] = [
  {
    desc: "basic",
    lines: [
      {
        commands: []
      }
    ]
  }
];

export default saposScripts;
