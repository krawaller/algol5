import { TowersDefinition } from "./_types";

const towersScripts: TowersDefinition["scripts"] = [
  {
    desc: "basic",
    lines: [
      {
        commands: []
      }
    ]
  }
];

export default towersScripts;
