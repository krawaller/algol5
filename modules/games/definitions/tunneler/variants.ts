import { TunnelerDefinition } from "./_types";

const tunnelerVariants: TunnelerDefinition["variants"] = [
  {
    ruleset: "basic",
    board: "basic",
    setup: "basic",
    desc: "regular",
    code: "r",
    arr: {
      setup: {},
      marks: [],
      potentialMarks: []
    }
  }
];

export default tunnelerVariants;
