import { TyrannosDefinition } from "./_types";

const tyrannosScripts: TyrannosDefinition["scripts"] = [
  {
    desc: "basic",
    lines: [
      {
        commands: []
      }
    ]
  }
];

export default tyrannosScripts;
