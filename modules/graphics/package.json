{
  "scripts": {
    "clean": "rm -rf dist; mkdir dist; mkdir dist/boards",
    "copypieces": "cp -r pieces-svg dist/pieces",
    "build": "npm run clean && npm run copypieces && ts-node makeboards"
  },
  "dependencies": {
    "canvas": "^2.4.1"
  },
  "devDependencies": {
    "@types/node": "^10.12.18",
    "ts-node": "^8.1.0",
    "typescript": "^3.4.4"
  }
}
