{
  "scripts": {
    "clean": "rm -rf dist; mkdir dist;",
    "copypieces": "cp -r pieces-svg dist/pieces",
    "build": "npm run clean && npm run copypieces && ts-node makeboards",
    "makeSVG": "ts-node ./commands/makeSVG"
  },
  "dependencies": {
    "canvas": "^2.4.1"
  },
  "devDependencies": {
    "@types/fs-extra": "^5.0.5",
    "@types/node": "^10.12.18",
    "fs-extra": "^7.0.1",
    "mini-svg-data-uri": "^1.0.3",
    "ts-node": "^8.1.0",
    "typescript": "^3.4.4"
  }
}
