import fs from "fs-extra";
import path from "path";
import prettier from "prettier";
import { AlgolArticleData } from "../../../types";

const tagFolder = path.join(__dirname, "../../pages/tags");

export const makeTagPage = (data: AlgolArticleData) => {
  const content = `
  // Generated by the makeTagPage command
  
  import React, { Fragment } from "react";
  
  import Head from "next/head";
  import { ArticlePage } from "../../../../ui/src/components/ArticlePage";
  import { pageActions } from "../../../helpers";
  import { tag } from "../../../../content/dist/tags/${data.id}/tag";
  
  export const Article = () => {
    const title = \`${data.title}\`;
    const crumbs = [{ content: "Tags", url: "/tags" }, { content: "${data.title}" }];
    return (
      <Fragment>
        <Head>
          <meta property="og:site_name" content="Chessicals"/>
          <meta property="og:image" content="${data.mainImage}" />
          <meta property="og:title" content={title} />
          <meta property="og:description" content="${data.blurb}"/>
          <title>{title}</title>
        </Head>
        <ArticlePage
          crumbs={crumbs}
          title={title}
          actions={pageActions}
          html={tag}
        />
      </Fragment>
    );
  };
  
  export default Article;
  `;
  const out = path.join(tagFolder, data.slug);
  fs.emptyDirSync(out);
  fs.writeFileSync(
    path.join(out, "index.tsx"),
    prettier.format(content, { filepath: "foo.ts" })
  );
  console.log("Made tag page for", data.id);
};
