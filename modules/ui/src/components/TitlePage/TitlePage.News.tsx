import React, { FunctionComponent } from "react";

export const TitlePageNews: FunctionComponent = () => {
  return <div>This'll be a straight-up version history. I think.</div>;
};

export default TitlePageNews;
